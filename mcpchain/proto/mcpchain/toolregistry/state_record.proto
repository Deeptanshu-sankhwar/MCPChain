syntax = "proto3";

package mcpchain.toolregistry;

option go_package = "mcpchain/x/toolregistry/types";

message StakeRecord {
  uint64 server_id = 1;
  string amount = 2;
  string staker_address = 3;
}
